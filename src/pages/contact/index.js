import { React } from 'react';
import '../../App.scss';
import './styles.scss';

import Header from '../../components/header';

function Contact() {
    return (
            <div id="homeBody">
                <Header />
                <section>
                    <h1>Contato</h1>
                </section>
            </div>
    )
} export default Contact;